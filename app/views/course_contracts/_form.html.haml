= form_for([:admin, @course_contract]) do |f|
  = f.error_messages
  
  .clearfix
    = f.label :name
    .input
      = f.text_field :name, :class => 'span6'
      
  .actions
    = f.submit nil, :class => 'btn primary'
